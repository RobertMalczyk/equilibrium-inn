__pycache__/
*.pyc
*.egg-info/
experiments/out/
equilibrium-engine/
.pytest_cache/

# Observatory build artifacts (regenerated by observatory/build_bundle.py and
# experiments/g2_parity.py). Source — build_bundle.py and assets/ — IS tracked.
observatory/*.html
!observatory/landing.html
observatory/*.zip
observatory/g2_reference.json
observatory/_stage/
observatory/_g2_ref/
observatory/_run_impulse/
observatory/_obs_demo/
observatory/run_fresh.html
observatory/pyodide/
observatory/_site/

# Source copy of the art pack (the working set lives in observatory/assets/).
Pictures/

# Local secrets — the optional M-H LLM seam reads these (never committed).
# A committed template lives at .env.example.
.env
